{% extends 'base.html.twig' %}

{% block body %}
	{% for product in products %}
		<li>
			<a href="{{ path('app_common_productdetails', { slug: product.slug}) }}">{{ product.title }}</a>
		</li>
	{% endfor %}
{% endblock %}
